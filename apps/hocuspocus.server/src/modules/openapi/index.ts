export { DOCS_PATH, SPEC_PATH } from './http/router'
// Registered above setupMiddleware, not returned by init — it takes no deps.
export { swaggerUiCsp } from './http/swaggerUi'
export { init } from './module'
