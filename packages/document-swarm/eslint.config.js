// Base config (not library): this is an internal operator CLI, so console output
// is the interface — the library preset's no-console rule doesn't apply here.
import baseConfig from '../eslint-config/index.js'

export default baseConfig
