export const useBookmarkTabData = () => {
  // This hook is intentionally empty to prevent automatic tab switching
  // The bookmark store already has a default activeTab of 'in progress'
  // and users should stay on their selected tab regardless of bookmark actions
}
