node_modules
dist
.DS_Store
*.log

# Cypress artifacts
cypress/screenshots
cypress/videos
cypress/downloads

# Generated by `release-prepack` (from @docs.plus/release-tooling) before each `bun publish` / `bun pm pack`.
# Single source of truth lives at the monorepo root.
# Anchored to package root so a vendored dep's nested LICENSE isn't silently ignored.
/LICENSE
