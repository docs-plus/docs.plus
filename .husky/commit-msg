#!/bin/sh

REPO_ROOT=$(git rev-parse --show-toplevel 2>/dev/null)
if [ -z "$REPO_ROOT" ]; then
    echo "❌ Could not resolve repository root. Commit message validation aborted."
    exit 1
fi

HOOK_SCRIPT="$REPO_ROOT/scripts/hooks/commit-msg.sh"
if [ ! -f "$HOOK_SCRIPT" ]; then
    echo "❌ Missing hook script: $HOOK_SCRIPT"
    exit 1
fi

exec sh "$HOOK_SCRIPT" "$@"
