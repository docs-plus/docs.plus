echo "🔍 Pre-push checks starting..."

# Build admin-dashboard (catches TS errors via Next.js build)
echo "📦 Building admin-dashboard..."
if bun run --filter "@docs.plus/admin-dashboard" build:local; then
    echo "✅ Admin dashboard build successful"
else
    echo "❌ Admin dashboard build failed. Push aborted."
    exit 1
fi

# Build webapp (catches TS errors via Next.js build)
echo "📦 Building webapp..."
if bun run --filter "@docs.plus/webapp" build:local; then
    echo "✅ Webapp build successful"
else
    echo "❌ Webapp build failed. Push aborted."
    exit 1
fi

# Note: hocuspocus runs TypeScript directly with Bun, no build step needed
# TypeScript errors are caught at runtime or via IDE

echo "🚀 All pre-push checks passed. Proceeding with push."
